import { GameController } from './components/GameController';

export const App = () => {
  return (
    <div className="w-full h-screen bg-black">
      <GameController />
    </div>
  );
};

export default App;